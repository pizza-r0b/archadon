// import React from 'react';

// const addClasses = (el, val, state) => {
//   const { props: { name } } = el;
//   const classes = [];
//   if (this.state.errors[name]) {
//     classes.push('input-error');
//   }
//   if (val) {
//     classes.push('input-filled');
//   }

//   return classes.length > 0 ? React.cloneElement(el, { className: classes.join(' ') }) : el;
// }
